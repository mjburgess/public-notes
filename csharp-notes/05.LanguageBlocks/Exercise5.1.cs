/*
In the Main Method of your Program:

Q. Create an empty array of 3 strings called people.

Q. Using a for loop accept a name at a time (Console.ReadLine()) 
    and add it to the array.
    HINT: people[i] = ..


Q. Use a foreach() loop to iterate over the array and print each name out. 

Q. use int.Parse() and Console.ReadLine() to accept an integer called command.

Q. use a switch() statement to test the value of command,
	 if it is 1 loop over the names again;
	 if it is 2 run the program again (ie., call Main())
	 
	 for any other value print "not recognised".
	 
*/
